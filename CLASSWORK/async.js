
// implementation of map
//const callback=(length)=>`OMG my name is ${length} char long`;

const strLength=(name,callback)=>{

  let length=name.length;
  console.log(callback(length));
}
 strLength('vikas',(length)=>`OMG my name is ${length} char long`)



 

